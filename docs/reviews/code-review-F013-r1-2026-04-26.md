# Code Review — F013-A r1 (Skill Mining Push)

- **日期**: 2026-04-26
- **审阅人**: Cursor Agent (auto-streamlined per F011/F012 mode)
- **范围**: T1-T5 src 修改 (5 新模块 + 3 文件改动 + 4 CLI handler + 1 perf script)

## Verdict: APPROVED

## 改动摘要

| 模块 | 增量 LOC | 变更类型 |
|---|---|---|
| `src/garage_os/skill_mining/__init__.py` | +20 (新文件) | 顶级包导出 |
| `src/garage_os/skill_mining/types.py` | +50 (新文件) | SkillSuggestion + SkillSuggestionStatus enum |
| `src/garage_os/skill_mining/suggestion_store.py` | +180 (新文件) | 5 status subdir CRUD + atomic write + mv |
| `src/garage_os/skill_mining/pattern_detector.py` | +250 (新文件) | 聚类 + 评分 + 双源 + already-covered |
| `src/garage_os/skill_mining/template_generator.py` | +220 (新文件) | 6 章节 in-memory string + minimal fallback |
| `src/garage_os/skill_mining/pipeline.py` | +220 (新文件) | SkillMiningHook + audit/decay + status summary |
| `src/garage_os/runtime/session_manager.py` | +12 | Cr-1 Path 1 hook 接入 (try/except) |
| `src/garage_os/ingest/pipeline.py` | +13 | Cr-1 Path 2 hook 接入 (try/except) |
| `src/garage_os/cli.py` | +280 | skill subparser + 2 handler + _print_skill_mining_status |
| `scripts/skill_mining_perf_smoke.py` | +75 (新文件) | CON-1303 manual prof |

**总增量**: ~1320 LOC src + ~100 LOC scripts/docs

## 设计-代码对齐性 (检查 ADR-D13-1..7 r2)

- ✓ ADR-D13-1 (顶级包): `src/garage_os/skill_mining/` 平级于 `knowledge/` / `memory/` / `sync/`
- ✓ ADR-D13-2 r2 (5 status subdir + mv): `_STATUS_SUBDIRS` 5 项; `move_to_status` 用 write + unlink 模拟 mv (跨子目录场景)
- ✓ ADR-D13-3 r2 (双 caller hook): Path 1 (`session_manager.py:262-271`) + Path 2 (`ingest/pipeline.py:120-130`); 都 try/except 包裹
- ✓ ADR-D13-4 r2 (双源 problem_domain_key): `extract_problem_domain_key` 实现 ExperienceRecord 直读 + KnowledgeEntry front_matter / topic fallback / None skip
- ✓ ADR-D13-5 (f-string 拼接): `template_generator.py` 全 f-string + collections.Counter, 无 Jinja2
- ✓ ADR-D13-6 (inline status): `_print_skill_mining_status` 在 `_status` 末尾, 与 `_print_sync_status` 同位置
- ✓ ADR-D13-7 (任务 1:1): T1-T5 commit 与 design § 8 任务表 1:1

## 代码质量

- 类型注解: 完整 (新代码 100% type-annotated; 数据类使用 `@dataclass`)
- Docstring: 全部公共函数有 docstring 引 FR + ADR
- 异常处理: try/except 一致包装 caller hook (`_trigger_memory_extraction`, `ingest pipeline`); pattern_detector / template_generator 内部 OSError 安全降级
- I/O abstraction: `garage_dir` 通过参数注入, 测试可用 tmp_path
- B5 user-pact opt-in: `--yes` (promote/suggest --purge-expired) + `--dry-run` (promote) + `--reject` (promote) 全显式
- 无 dead code, 无 commented-out 代码
- ruff baseline diff: **0** (T1-T5 新代码引入 0 新 ruff 错误; pre-existing 478 错误模式完全不变)

## 红线确认

- ✓ CON-1301 (F003-F011 既有 API 不破): F003 既有 method 签名字节级不变; caller 改动全 try/except 非 breaking
- ✓ CON-1302 (零依赖变更): `git diff main..HEAD -- pyproject.toml uv.lock` = 0
- ✓ CON-1303 (perf < 5s): manual smoke 1000+1000 = 0.803s (余 84%)
- ✓ CON-1304 (promote 不动 pack.json): byte-level sentinel + code review (`_skill_promote` 仅 `Path("packs/...").write_text`, 不调 pack_install / pack.json mutation API)
- ✓ CON-1305 (echo 不自动 invoke): `_skill_promote` 末尾 print echo + return 0; 不 spawn subprocess / 不 import hf-test-driven-dev module

## 残余项

- **None blocking**. F011-pattern E402/I001/F841 的 3 个 ruff 错误是 pre-existing 模式 (uninstall handler 同款), F013-A 引入 0 新错误.

## 通过条件

✅ code review APPROVED, 进入 traceability review.
