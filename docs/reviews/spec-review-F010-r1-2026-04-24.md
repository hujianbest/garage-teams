# Spec Review — F010 Garage Context Handoff (`garage sync`) + Host Session Ingest (`garage session import`) (r1)

- **Target**: `docs/features/F010-garage-context-handoff-and-session-ingest.md` (草稿 r1)
- **Reviewer**: Independent generalPurpose subagent (read-only, hf-spec-review SKILL)
- **Date**: 2026-04-24
- **关联 PR**: #28 (待创建; 分支 `cursor/f010-context-handoff-and-session-import-bf33`)
- **关联上游**: `docs/planning/2026-04-24-vision-gap-and-next-cycle-plan.md` § 2.1

## Verdict: **需修改** (revise)

1 critical (CLI/目录引用与既有契约事实不符) + 5 important + 6 minor，全部 LLM-FIXABLE，0 USER-INPUT，1 轮定向回修可闭合。

verdict 理由：F010 spec 范围清晰、双子命令拆分（A=sync push、B=ingest pull）锚定明确、与 F003/F006/F007/F009 既有契约边界完整、HYP 全部带 Type/Confidence/Validation/Blocking 标注、CON-1004（B5 user-pact）守门精神显式、deferred backlog 完整。但 spec 5 处反复引用一个不存在的 CLI 子命令 `garage knowledge approve <candidate-id>` 与不存在的目录 `.garage/knowledge/.candidates/`，与实测 `src/garage_os/memory/candidate_store.py` (`BATCHES_DIR = "memory/candidates/batches"` + `ITEMS_DIR = "memory/candidates/items"`) + `src/garage_os/cli.py:662 _memory_review` 既有契约（实际入口是 `garage memory review <batch-id> --action accept --candidate-id ...`）冲突，下游 design 与测试将照抄非真实契约 — 这是 spec 必须 1 轮回修的事实硬伤。

## Findings

### Critical (1)

**C-1**: `garage knowledge approve <candidate-id>` 与 `.garage/knowledge/.candidates/` 是 spec 自创、与既有实现完全不符 [`critical`][`LLM-FIXABLE`][`A5/C2`]
- 锚点 (5 处): `§ 2.2 #5` / `SM-1002` / `FR-1005 acceptance` / `FR-1006 acceptance` / `CON-1004`
- 实际契约 (实测):
  - CLI: `garage memory review <batch-id> --action accept --candidate-id <candidate-id>` (`src/garage_os/cli.py:662 _memory_review`)
  - 目录: `.garage/memory/candidates/items/` (draft) + `.garage/memory/candidates/batches/` (queue) (`src/garage_os/memory/candidate_store.py:16-17`)
- 修复指引: 全文 5 处替换 + CON-1004 显式声明"复用 F003/F004 既有 candidate→memory review→publisher 链路, 0 新增 CLI 子命令"

### Important (5)

**I-1**: HYP-1004 / HYP-1006 conversation history 路径声称稳定但缺 URL / 本机 ls evidence anchor (与 F009 spec § 2.3 三家官方文档 URL 对照不齐) [`important`][`LLM-FIXABLE`][`C7/A5`]

**I-2**: FR-1005 / FR-1006 仅写 happy path, 缺 5 类负路径 acceptance (空目录 / schema 损坏 / unknown host / interactive 取消 / batch 部分失败) [`important`][`LLM-FIXABLE`][`Q3/A6/C2`]

**I-3**: FR-1004 + CON-1003 缺 `.mdc` 文件 front matter 契约 (与 HYP-1002 + § 2.2 #3 + Cursor `alwaysApply: true` 自动加载机制冲突) [`important`][`LLM-FIXABLE`][`Q3/A3/C2`]

**I-4**: FR-1009 sync 段与 F009 既有 `garage status` 输出 ordering / fallback 未约束 (CON-1001 字节级守门有缝隙) [`important`][`LLM-FIXABLE`][`C2/Q3`]

**I-5**: sync-manifest.json schema 在 spec 散点提及但无统一最小字段表 anchor (design ADR 第一动作必然回 spec) [`important`][`LLM-FIXABLE`][`C2/C5`]

### Minor (6)

- **M-1**: FR-1006 non-TTY 退化 wording 与 F009 FR-703 不字面对齐
- **M-2**: FR-1008 stdout marker 含 `+` 号但未约束 grep ERE escape 习惯
- **M-3**: FR-1010 文档同步范围未涵盖 `packs/README.md` (与 F009 FR-910 对齐与否未声明)
- **M-4**: CON-1002 "F003-F006 0 改动" 与 SM-1002 "F003 提取 log 含 import 来源 anchor" 隐性冲突
- **M-5**: HYP-1007 (Value hypothesis) 缺 measurement anchor
- **M-6**: FR-1004 path 表用 `Path(...)` 构造器是轻微设计泄漏 (应字符串路径形态, 与 F009 FR-904 / F007 FR-704 对齐)

## 量化覆盖度

| 维度 | 应有 anchor | 实际 anchor | 通过率 |
|---|---|---|---|
| FR (1001..1010) | 10 | 10 | 100% |
| NFR (1001..1004) | 4 | 4 (ISO 25010 + QAS 五要素 + Response Measure 阈值齐全) | 100% |
| CON (1001..1007) | 7 | 7 | 100% |
| ASM (1001..1004) | 4 | 4 (含失效影响 + Validation) | 100% |
| HYP (1001..1008) | 8 | 8 (Blocking 6 / Low confidence 1 allow deferred) | 100% |
| SM (1001..1004) | 4 (含 Non-goal Metrics) | 4 | 100% |
| INVEST | 全部 | 全部 (Independent / Small / Testable 通过) | 100% |
| EARS / BDD / MoSCoW / ISO 25010 / QAS | 全部 | 全部 (仅 FR-1005/1006 BDD 缺负路径, important #2) | 95% |

## Vision-gap traceability

✅ **完整回应** vision-gap planning § 2.1 P0:
- F010-A → FR-1001/2/3/4/7/8 + NFR-1001/2/3/4 + CON-1001/3/5/6
- F010-B → FR-1005/6 + CON-1004/7 + HYP-1004/5/6
- 不夹带 F011 内容 (style 维度 / 2 production agents / pack install)

但 critical finding 弱化 SM-1002 可验证性 → 修后即充分.

## CON-1004 (B5 user-pact "你做主") 守门评估

⚠️ **机制充分**, **API 锚点不充分** (因 critical finding):
- 默认 interactive + `--all` opt-in: ✓
- 不绕过 candidate 审批: ✓ (机制对)
- 但 spec 写的 "garage knowledge approve" 是 fictional CLI; 实际 `garage memory review` 才是真实候选审批入口
- 修 critical 后即充分

## Blocking 假设处理

✅ **整体合理**:
- HYP-1001/2/3/4/6/8 = Blocking + manual smoke 验证 = 与 vision-gap P0 定级一致
- HYP-1005 = allow deferred + CON-1007 显式降级路径合理

仅 HYP-1004/1006 缺 spec 阶段第一道 evidence anchor (important I-1).

## scope 与 F009 复用

✅ **清晰**:
- CON-1006 复用 F009 既有 `HostInstallAdapter` Protocol (实测 `host_registry.py` Protocol 已可扩展)
- FR-1002 与 F009 per-host scope override 解析 100% 一致
- ingest 路径不需要 scope 显式声明 (host history 路径是宿主自己定的)

## 是否需要回到 hf-specify

✅ **是** — 1 轮定向回修可闭合 critical + 5 important + 6 minor 全部 finding.

## Structured Return

```json
{
  "conclusion": "需修改",
  "verdict": "REJECT (1 round revise)",
  "next_action_or_recommended_skill": "hf-specify",
  "needs_human_confirmation": false,
  "reroute_via_router": false,
  "finding_count": {"critical": 1, "important": 5, "minor": 6},
  "all_llm_fixable": true,
  "user_input_count": 0
}
```
