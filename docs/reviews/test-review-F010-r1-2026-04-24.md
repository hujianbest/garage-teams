# Test Review F010 r1 — 2026-04-24

- **Reviewer**: Independent generalPurpose subagent (read-only, hf-test-review SKILL)
- **Target**: F010 cycle test suite (T1-T7 + manual smoke walkthrough)

## Verdict: **通过 (PASS)** with findings

Test baseline 715 → 821 passed (+106, 0 regressions). 全部 FR/NFR/CON/INV anchor 至少一个测试覆盖. 0 critical / 1 important / 6 minor / 4 nit, 全部 LLM-FIXABLE 且不阻塞 hf-code-review.

## Coverage Matrix

| Spec/Design Anchor | Coverage |
|---|---|
| FR-1001..1009 | ✓ (sync compiler/manifest/pipeline/CLI tests + ingest pipeline/CLI tests) |
| FR-1010 | ⚠️ docs sync 仅人工 grep verify (MIN-3 minor) |
| NFR-1001 (dogfood 不变性) | ✓ INV-F10-2 sentinel + manual smoke Track 1 |
| NFR-1002 (mtime stability) | ✓ test_pipeline_idempotent.py |
| NFR-1003 (user content preserved) | ✓ test_pipeline_user_content_preserved.py |
| NFR-1004 (perf ≤ 5s) | ⚠️ 仅 manual smoke 验 (MIN-2 minor) |
| CON-1001 (F009 字节级兼容) | ✓ INV-F10-2 sentinel |
| CON-1002 (F003-F006 0 改动) | ✓ test_session_provenance_via_metadata + 既有 F003 baseline 0 退绿 |
| CON-1003 (marker 圈定) | ✓ test_render_markdown.py |
| CON-1004 (B5 user-pact) | ✓ test_pipeline_candidate_path.py (走 candidate 而非直接入库) |
| CON-1005 (manifest 隔离) | ✓ INV-F10-6 test in test_pipeline_three_way_hash.py |
| CON-1006 (Protocol 字段扩展) | ✓ test_context_path_three_hosts.py |
| CON-1007 (cursor deferred) | ✓ test_cursor_deferred.py + INV-F10-9 |
| INV-F10-1..10 | ✓ 全部覆盖 (含 sentinel) |

## Findings

### Important (1)

- **IMP-1**: `tests/ingest/test_e2e_import_then_sync.py` 在 task plan § T6 + design § 4.2 列了但实际未落地; SM-1002 round-trip (`import → candidate → memory review accept → publisher → sync → host context surface`) 因此没有自动化 e2e. **不阻塞 hf-code-review** (可在 hf-finalize 阶段补; manual smoke Track 4 已用 stand-in 覆盖大部分 SM-1002 语义)

### Minor (6)

- **MIN-1**: INV-F10-2 sentinel subprocess fork 让 CI wall-clock 翻倍 (~50s → ~100s); 可通过 process-pool / pytest plugin 优化 (F011+ candidate)
- **MIN-2**: NFR-1004 perf budget ≤ 5s 仅 manual smoke 验, 无自动 assert
- **MIN-3**: FR-1010 docs cold-read 5 min 链 仅人工 grep verify, 无自动 doctest
- **MIN-4**: ClaudeCodeHistoryReader.max_entries=30 边界未跑 (fixture 仅 1+1 broken)
- **MIN-5**: OpenCode reader 测试比 Claude 浅 (无 helper test, 无 derived_tags test)
- **MIN-6**: Track 4 closing leg 用 dogfood edit 作 stand-in (本应 candidate review 后); honest disclosure ✓

### Nit (4)

- 文件名差异 (`test_manifest.py` vs `test_sync_manifest.py` plan 但 sync/ 目录已带前缀)
- `test_manifest.py::TestStableOrdering` 用 positional args 略 fragile
- `tests/sync/test_baseline_no_regression.py` 缺 module docstring 关于 CI cost
- conversation fixture 文件名 `conversation-002-broken.json` 含连字符 (与 conversation-001 风格一致 ✓ 实际 nit pass)

## Structured Return

```json
{
  "conclusion": "通过",
  "verdict": "PASS_WITH_FINDINGS",
  "next_action_or_recommended_skill": "hf-code-review",
  "needs_human_confirmation": false,
  "reroute_via_router": false,
  "finding_count": {"critical": 0, "important": 1, "minor": 6, "nit": 4},
  "all_llm_fixable": true,
  "test_baseline": "715 → 821 passed (+106, 0 regressions)",
  "carry_forward_to_finalize": ["IMP-1: add tests/ingest/test_e2e_import_then_sync.py (SM-1002 round-trip automation)"]
}
```
