# Code Review F010 r1 — 2026-04-24

## Verdict: **需修改 (CHANGES_REQUESTED)** — 2 important + 6 minor + 5 nit, 全 LLM-FIXABLE

架构清晰: 13 ADR + 5 CON + 真实 API 字面对齐. 2 important findings 都是单文件 1-2 行可定向回修.

## Important (2)

- **IMP-1** [CR3]: `_sync` 未 catch `SyncManifestMigrationError` (cli.py:518-533) — 损坏 sync-manifest 致 uncaught traceback. `_print_sync_status` 已守门, `_sync` 缺同等防御.
- **IMP-2** [CR1/CR2]: `_compose_mdc_content` (pipeline.py:319-321) 在 cursor `.mdc` 已存在 + 无 marker + 有用户内容路径丢失 `alwaysApply: true` front matter — 违反 FR-1004 + HYP-1002 失效.

## Minor (6) / Nit (5) — carry-forward to finalize

均为 LLM-FIXABLE (size_budget hardcode / batch-id None render / size_kb truncation / docstring 完善 / lazy import / `_now_iso` 重复等).

## Architectural Health

- 13/13 ADR 落地 ✓
- CON-1001/1002/1005/1006/1007 全部守门 ✓
- 真实 API 字面对齐 (update_session(context_metadata=) / archive_session(reason=) / extract_for_archived_session_id) ✓ — 无 design r2 typo 重现

## Structured Return

```json
{
  "conclusion": "需修改",
  "verdict": "CHANGES_REQUESTED",
  "next_action_or_recommended_skill": "hf-test-driven-dev",
  "needs_human_confirmation": false,
  "reroute_via_router": false,
  "finding_count": {"critical": 0, "important": 2, "minor": 6, "nit": 5},
  "all_llm_fixable": true,
  "in_cycle_fix": ["IMP-1: _sync catch SyncManifestMigrationError", "IMP-2: cursor .mdc fallback path inject front matter"],
  "carry_forward_to_finalize": ["MIN-1..6 + 5 nit (cli/pipeline 渲染细节, 不阻塞)"]
}
```
