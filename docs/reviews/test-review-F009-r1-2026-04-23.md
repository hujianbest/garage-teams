# Test Review — F009 `garage init` 双 Scope 安装 + 交互式 Scope 选择 (r1)

- **日期**: 2026-04-23
- **Reviewer**: Independent generalPurpose subagent (read-only, hf-test-review SKILL)
- **关联 PR**: #24 (`cursor/f009-init-scope-selection-bf33`)
- **关联 spec**: `docs/features/F009-garage-init-scope-selection.md` (10 FR + 4 NFR + 4 CON + 4 ASM)
- **关联 design**: `docs/designs/2026-04-23-garage-init-scope-selection-design.md` (11 ADR + 9 INV)
- **关联 tasks**: `docs/tasks/2026-04-23-garage-init-scope-selection-tasks.md` (6 task)
- **关联 manual smoke**: `docs/manual-smoke/F009-walkthrough.md`

## Verdict: **APPROVE_WITH_FINDINGS**

测试质量 r1 通过, **6 维评分均分 7.50/10**, 无 critical / 阻塞性 finding. 10 FR + 4 NFR + 4 CON + 9 INV 全部至少 1 个测试 anchor; 11 个新增测试文件 + 1 baseline JSON + 3 carry-forward wording 修复 + 4 manual smoke 轨道 + 7 类边界覆盖; 测试基线 708 passed (+75, 0 退绿). 可直接派发 `hf-code-review`, 无需回到 `hf-test-driven-dev` 修测试.

## 量化覆盖度

| 维度 | 应有 anchor | 实际 anchor | 通过率 |
|---|---|---|---|
| FR | 10 (FR-901..910; FR-910 docs 仅人工 review) | 10 | 100% |
| NFR | 4 (NFR-901..904) | 4 | 100% |
| CON | 4 (CON-901..904) | 4 (CON-902 守门偏弱, important) | 100% |
| INV | 9 (INV-F9-1..9) | 9 (INV-F9-2 守门偏弱, important) | 100% |
| 边界用例 | 7 类 | 7 类全部覆盖 | 100% |
| Fixture isolation (INV-F9-8) | monkeypatch Path.home() | 5 文件统一模式 | 100% |

## Findings 汇总

### Critical (0)

无.

### Important (2) — 不阻塞 hf-code-review, 可作为 hf-finalize 阶段或下 cycle carry-forward

**I-1**: **CON-902 / INV-F9-2 phase 1+3 算法守门偏弱** (相关任务: T2)
- 当前实现: `test_pipeline_scope_routing.py::TestPhase1Phase3AlgorithmInvariance` 仅用 `inspect.signature` 比对函数签名, 未用 `inspect.getsource` 比对函数 body 字节级
- spec 期望: CON-902 "phase 1 + phase 3 算法主体严格不变" 应当对 body 也守门, 防止 cycle 内业务逻辑改写
- 建议: hf-finalize 后 carry-forward 加 `inspect.getsource(_check_conflicts)` 与 git baseline body 字节级比对

**I-2**: **FR-901 验收 #4 退出码偏离** (相关任务: T4)
- spec 字面: "`--scope unknown` → exit 1 + stderr 'Unknown scope: unknown'"
- 实施实际: argparse choices=['project', 'user'] 在 parse 阶段拒绝, SystemExit(2)
- test 已 carry-forward: `test_unknown_scope_argparse_rejected` 实测 SystemExit(2)
- 评估: argparse choices 是更早期 + 更严格的拒绝, 与 spec 验收语义一致 (拒绝 invalid scope), 仅退出码差异 (2 vs 1)
- 建议: 在 spec 加 USER-INPUT 标注 (argparse 优于自定义 fallback parsing) 或在 hf-finalize approval 文档中记录此 deliberate 偏离

### Minor (10) — 不阻塞, 仅供下 cycle 或 hf-finalize 参考

**M-1**: stdout wording 与 ADR-D9-7 sample 字面 100% 一致性未守门 — `test_status_project_scope_only` 仅 grep 关键 substring; ADR-D9-7 内 sample wording 未做字面比对
**M-2**: `UserHomeNotFoundError` 类型存在但 runtime catch 路径未测 — F009 实施依赖 `Path.home()` 抛 RuntimeError, 但实际跑 fake_home 不会触发; 仅 issubclass 守门
**M-3**: T1 task plan § 5 设计种子列的 `test_assert_fires_on_violation` 未落地 — host_id 含 `:` 时 import-time AssertionError 实际靠 import 时触发, 无 explicit 单元测试
**M-4**: FR-908 mixed-scope status 顺序未守门 — ADR-D9-7 说 "先 project, 再 user", `test_status_project_scope_only` 没测 mixed scope 顺序
**M-5**: T5 sentinel 无独立 RED 演示 — baseline JSON 与 sentinel test 同 commit 落地, 无故意 RED 阶段; 与 F008 T1c (sentinel 反向同步) 同源
**M-6**: idempotent re-run 跨 scope 未显式测 — install_packs 二次跑同一 scope 行为有测试 (F007 既有), 但 "先装 project, 再装 user" 二次跑互不污染未显式测
**M-7**: dogfood baseline self-validating 风险 — `test_dogfood_invariance_F009` 用 install_packs 写 actual + 比 baseline; 若 install_packs bug 同时写 actual + baseline 则 sentinel 失效 (本测试无独立 source-of-truth)
**M-8**: CON-904 v2→v1 reverse migration 显式测试缺失 — spec 说 "schema 升级单向", 没有测试守门 v2 manifest 误降 v1 时的行为
**M-9**: `prompt_scopes_per_host` EOF 第二段缺失 host 兜底 — 测试 `test_eof_per_host_round_defaults_remaining_project` 仅测 cursor 单一余下 host; 多 host 余下 EOF 时是否全部 default project 未测
**M-10**: `test_full_init_user_scope.py` 未跑 manifest 全字段 round-trip — 验证了 scope 字段 + dst absolute, 但 schema_version=2 + content_hash 与 dogfood baseline content_hash 一致性未交叉验证

## Sentinel test fail-first 演示证据

| Sentinel | 类型 | RED 演示 | 评估 |
|---|---|---|---|
| `test_dogfood_invariance_F009` | dogfood SHA-256 | 无 (M-5) | minor; baseline JSON 与 sentinel 同 commit 落地是 F008 T1c 同精神, 可接受 |
| `test_skill_anatomy_drift` (F008 既有) | byte-equal | F008 T1c 已演示 RED → GREEN | ✓ |
| `test_host_registry_colon_assert` | import-time guard | 实时守门, 但单元测试未 enum (M-3) | minor |

## 边界用例覆盖

| 边界 | 覆盖 |
|---|---|
| EOF (interactive 第一轮 + 第二段) | ✓ test_interactive_two_round.py::TestPromptScopesPerHostEOF |
| non-TTY 退化 | ✓ test_interactive_two_round.py::TestPromptScopesPerHostNonTTY |
| Mixed scope per-host | ✓ test_pipeline_scope_routing + test_full_init_user_scope |
| Cross-scope no-conflict (FR-907) | ✓ test_pipeline_scope_routing.py::TestPhase3ConflictsCrossScope |
| Manifest corruption (CON-904) | ✓ test_manifest_migration.py::TestSafetySemantics (含 SHA-256 + mtime 双重守门) |
| Unsupported schema_version | ✓ test_manifest_migration.py::test_unsupported_schema_version_raises_no_overwrite |
| Empty host_ids | ✓ test_interactive_two_round.py::TestPromptScopesPerHostEmpty |
| Cursor user no-agent surface | ✓ test_pipeline_scope_routing.py::test_resolve_targets_cursor_user_scope_no_agent |
| Unknown scope (CLI) | ✓ test_cli.py::test_unknown_scope_argparse_rejected + test_per_host_override_unknown_scope_exits_1 |

## Fixture isolation (INV-F9-8)

5 个 user scope 测试文件统一使用 `monkeypatch.setattr(Path, "home", classmethod(lambda cls: fake))` 模式:
- `test_adapter_user_scope.py`
- `test_pipeline_scope_routing.py`
- `test_cli.py::TestInitWithScope`
- `test_manifest_v2_dogfood_fields_stable.py`
- `test_full_init_user_scope.py`

→ **严格守门**, 不污染真实 `~/`. 评分: 10/10.

## 下一步

✅ **不需回 hf-test-driven-dev 修测试**. 派发 `hf-code-review` reviewer subagent.

2 条 important finding (I-1 CON-902 body 守门 + I-2 退出码偏离) 可作为:
- hf-finalize 阶段在 approval 文档中记录的 "deliberate 偏离 / 下 cycle carry-forward"
- 或下个 cycle (F010+) 的开场任务

10 条 minor 不阻塞, 仅供下 cycle 参考.

## Structured Return

```json
{
  "conclusion": "通过",
  "verdict": "APPROVE_WITH_FINDINGS",
  "next_action_or_recommended_skill": "hf-code-review",
  "needs_human_confirmation": false,
  "reroute_via_router": false,
  "finding_count": {"critical": 0, "important": 2, "minor": 10},
  "测试覆盖度": {"FR": "10/10", "NFR": "4/4", "CON": "4/4", "INV": "9/9", "边界": "7/7"},
  "测试基线": "708 passed (+75 from F008 baseline 633, 0 regressions)"
}
```
