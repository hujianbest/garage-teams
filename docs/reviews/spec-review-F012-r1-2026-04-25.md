# Spec Review F012 r1 — 2026-04-25

- **Reviewer**: Independent generalPurpose subagent (read-only, hf-spec-review SKILL)
- **Target**: `docs/features/F012-pack-lifecycle-completion.md` r1

## Verdict: **通过 (APPROVE_WITH_FINDINGS)** — 0 critical

11 finding (0 critical / 1 mechanical / 5 important / 5 minor); 全部 LLM-FIXABLE 或简单 wording 决策. 真实 API 锚点 **8/8 命中**, 无 F010 r1 typo 重现. 范围严格按 planning § 2.3 锁定.

## API anchor check (F010 r1 typo 防护)

| spec 锚点 | 实测位置 | 字面 |
|---|---|---|
| `install_pack_from_url` | `pack_install.py:47` | ✅ |
| `list_installed_packs` | `pack_install.py:157` | ✅ |
| `PackInstallSummary` / `PackInstallError` | `pack_install.py:35,40` | ✅ |
| `ManifestFileEntry(src,dst,host,pack_id,content_hash,scope)` | `manifest.py:78-84` | ✅ |
| `_sync_skill_sidecars(skill_src_dir, skill_dst_dir)` | `pipeline.py:490` | ✅ (含 references/assets/evals/scripts 4 子目录) |
| `KnowledgeStore.list_entries(knowledge_type=...)` | `knowledge_store.py:229` | ✅ |
| `_MIGRATION_REGISTRY` / `register_migration(from_major, to_major)` | `version_manager.py:114, 117` | ✅ |
| `migrate_v1_to_v2(prior_v1, workspace_root)` | `manifest.py:175` | ✅ |

## Findings

### Mechanical (1)

- **M-1** [LLM-FIXABLE]: §2.1 E narrative line 73 提 "VersionManager.SUPPORTED_VERSIONS 同步加 2", 但 FR-1214 statement / acceptance 未提及. 修 FR-1214 显式包含或删 §2.1 line 73 (推荐前者).

### Important (5)

- **I-1** [LLM-FIXABLE]: FR-1207 publish 默默改写 pack.json source_url 缺 user-pact 告知. 在 prompt 文案显式列出 + 加 `--no-update-source-url` flag 兜底
- **I-2** [LLM-FIXABLE]: FR-1207 `git push --force` 风险未在 prompt 中明示. 加 "WARNING: force push will overwrite existing remote content" + 检测 `git ls-remote` 非空时二次确认
- **I-3** [LLM-FIXABLE]: FR-1208 sensitive scan 二进制文件边界未声明. 加 extension allowlist 或 content sniff first 1024 bytes
- **I-4** [USER-INPUT → can self-decide in auto mode]: SM-1204 / HYP-1204 / ASM-1204 三处脱敏度量口径不一致. 推荐合并 wording 为 "5 类规则全部命中 fixture (作为召回率代理度量, 不追求 100% 召回, B5 用户兜底)"
- **I-5** [LLM-FIXABLE]: FR-1207 `--force` (sensitive scan 跳过) 与 `--yes` (主 prompt 跳过) 语义重叠不清晰. 加状态表 (4 flag 组合 × 2 sensitive 命中 = 8 行) 明确

### Minor (5)

- **Mi-1** [LLM-FIXABLE]: FR-1204 update prompt 缺独立 acceptance scenario; 加 BDD scenario "用户输入 'n' → exit 0 + packs 不变"
- **Mi-2** [LLM-FIXABLE]: FR-1209 publish `--dry-run` 与 prompt 顺序未声明. 加 "`--dry-run` 隐含 `--yes`"
- **Mi-3** [LLM-FIXABLE]: FR-1211 `<workspace>/exports/` 默认输出路径易被误 commit. 改 `~/.garage/exports/` 或加 `.gitignore` 自动 stage
- **Mi-4** [LLM-FIXABLE]: FR-1207 git commit author 写死. 加 `--commit-author` flag 或读 git global config (D-1217 candidate)
- **Mi-5** [LLM-FIXABLE]: FR-1211 KnowledgeStore.list_entries 与直接读文件路径混淆. 改写为 "用 KnowledgeStore.list_entries 拿 metadata + filesystem read 拿 markdown 原文 + 脱敏 + tarball"

## 守门核对 (全通过)

- ✅ CON-1202 + CON-1207 (F011/F003-F010 既有 0 改动)
- ✅ Vision-gap traceability (planning § 2.3 5 部分严格锁定, 不夹其它)
- ✅ HYP Blocking 5/6 (HYP-1204 Non-blocking, B5 兜底)
- ✅ § 11 BLK 4/4 答复 (BLK-1203 force-push 风险讨论待 I-2 补)
- ⚠️ B5 user-pact 5/7 (I-1 + I-2 待补强)
- ✅ Deferred backlog 7 项合理 (建议加 D-1217 publish commit author config)

## Structured Return

```json
{
  "conclusion": "通过",
  "verdict": "APPROVE_WITH_FINDINGS",
  "next_action_or_recommended_skill": "hf-design",
  "needs_human_confirmation": false,
  "reroute_via_router": false,
  "finding_count": {"critical": 0, "mechanical": 1, "important": 5, "minor": 5, "total": 11},
  "api_anchor_check": "8/8 verified",
  "user_input_count": 1,
  "llm_fixable_count": 10,
  "auto_mode_decision_for_I4": "Merge to '5 rules all hit (proxy for 80% recall), no 100% pursued, B5 fallback'"
}
```
