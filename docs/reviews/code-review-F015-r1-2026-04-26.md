# Code Review — F015 r1 (Agent Compose)

- **日期**: 2026-04-26
- **审阅人**: Cursor Agent (auto-streamlined per F011/F012/F013-A/F014 mode)
- **范围**: T1-T4 src 修改 (4 新模块 + 2 CLI handler + 3 sentinel)

## Verdict: APPROVED

## 改动摘要

| 模块 | 增量 LOC | 变更类型 |
|---|---|---|
| `src/garage_os/agent_compose/__init__.py` | +20 (新) | 顶级包 |
| `src/garage_os/agent_compose/types.py` | +50 (新) | AgentDraft + ComposeResult dataclass |
| `src/garage_os/agent_compose/template_generator.py` | +200 (新) | 7-section template + Ni-1 multi-line desc 切分 |
| `src/garage_os/agent_compose/composer.py` | +180 (新) | compose() + Im-1 r2 双层语义 |
| `src/garage_os/agent_compose/pipeline.py` | +60 (新) | compute_status_summary + FIRST_CLASS_PACKS |
| `src/garage_os/cli.py` | +220 | agent subparser + 2 handler + status 段 |

**总增量**: ~730 LOC src + 3 sentinel test files

## 设计-代码对齐性 (检查 ADR-D15-1..6)

- ✓ ADR-D15-1 (顶级包): `src/garage_os/agent_compose/` 与 sync/skill_mining/workflow_recall 平级
- ✓ ADR-D15-2 (schema 收窄至 2 agents): template 严格 7-section 遵 F011 blog-writing + code-review; garage-sample 排除
- ✓ ADR-D15-3 (multi-line desc 切分): `_extract_skill_summary` 处理 YAML | 多行 + 中英文句号 + max 80 chars
- ✓ ADR-D15-4 (双层 missing 语义): library 返 `ComposeResult(missing_skills, draft)` 仍生成 partial; CLI strict exit 1
- ✓ ADR-D15-5 (inline status): `_print_agent_compose_status` 在 `_status` 末尾 (与 F013-A / F014 同 pattern)
- ✓ ADR-D15-6 (T1-T4 1:1): T1-T4 commit 与 design § 8 1:1

## 代码质量

- 类型注解: 完整 (新代码 100% type-annotated)
- Docstring: 全部公共函数有 docstring 引 FR + ADR + spec/reviewer Cr-N r2 修订记录
- 异常处理: composer 包 OSError; CLI handler exit 1 友好 msg
- I/O abstraction: packs_root 通过参数注入, 测试用 tmp_path
- B5 user-pact: prompt 默认; --yes / --dry-run / --no-style 全显式; CON-1503 不动 pack.json
- 无 dead code, 无 commented-out 代码
- ruff baseline diff: **0** (T1-T4 引入 0 新 ruff 错误)

## 红线确认

- ✓ CON-1501 (F003-F014 既有 API 字节级不变): F015 不修改任何既有模块的 method 签名; CLI 加 sibling subparser 不破坏既有 dispatch
- ✓ CON-1502 (零依赖): `git diff main..HEAD -- pyproject.toml uv.lock` = 0
- ✓ CON-1503 (compose 不动 pack.json): byte sentinel PASS
- ✓ CON-1504 (agent.md 7-section schema): TestRenderFullSchema 9 用例覆盖每段
- ✓ CON-1505 (sibling subcommand 独立): `test_no_sibling_import.py` AST sentinel + code review (CLI handler 不 import recommend / skill_mining / workflow_recall)

## 残余项

- **None blocking**. F011-pattern E402/I001/F841 ruff 错误是 pre-existing 全局模式, F015 引入 0 新错误.

## 通过条件

✅ code review APPROVED, 进入 traceability review.
