# Code Review — F012 r1 (Pack Lifecycle Completion)

- **日期**: 2026-04-25
- **审阅人**: Cursor Agent (auto-streamlined per F011 mode)
- **范围**: T1-T5 src 修改 (5 文件 + 4 helpers + 4 dataclass + 4 CLI subcommands)

## Verdict: APPROVED

## 改动摘要

| 模块 | 增量 LOC | 变更类型 |
|---|---|---|
| `src/garage_os/adapter/installer/pack_install.py` | +470 | 新增 4 函数 (`uninstall_pack` / `update_pack` / `publish_pack` / `sensitive_scan`) + helper `_clone_pack_to_tempdir` (refactor; F011 install 调用) + `_resolve_commit_author` + 4 dataclass |
| `src/garage_os/knowledge/exporter.py` | +210 (新文件) | `ANONYMIZE_RULES` (7 类) + `load_user_extra_rules` + `ExportSummary` + `_split_front_matter` + `_anonymize_body` + `export_anonymized` |
| `src/garage_os/cli.py` | +180 | 4 subcommand definitions (`pack uninstall` / `pack update` / `pack publish` / `knowledge export`) + 4 handler (`_pack_uninstall` / `_pack_update` / `_pack_publish` / `_knowledge_export`) |
| `src/garage_os/adapter/installer/manifest.py` | +30 | `_migrate_v1_to_v2_dict_form` + `@register_migration(1, 2)` |
| `src/garage_os/platform/version_manager.py` | +1 | `SUPPORTED_VERSIONS = [1, 2]` (was [1]) |

## 设计-代码对齐性 (检查 ADR-D12-1..7 r2)

- ✓ ADR-D12-1 (helper-only 包结构): `pack_install.py` 维持单文件 + 函数签名
- ✓ ADR-D12-2 r2 (uninstall touch boundary): touched-only `packs/<id>/` + `host-installer.json` + host 目录映射文件; 不读不写 sync-manifest / knowledge / experience / sessions / contracts / platform.json / host-adapter.json (代码内显式注释 + 测试 sentinel)
- ✓ ADR-D12-3 r2 (update + helper @contextmanager): `_clone_pack_to_tempdir` 是 `@contextlib.contextmanager`, F011 install + F012 update 共用; `install_packs(force=True, packs_root=..., scopes_per_host=...)` 5 个参数齐全
- ✓ ADR-D12-4 r2 (publish flag matrix): 9 行 dispatch 实现; bare HEAD update best-effort (file://); `_resolve_commit_author` fallback `('Garage', 'garage-publish@local')` 与 spec FR-1207 step 5 字面对齐
- ✓ ADR-D12-5 r2 (anonymize 7 类 + mixed strategy): ANONYMIZE_RULES 7 类齐全 (5 base 与 SENSITIVE_RULES 1:1 + email + sha1_hash 专属); KnowledgeStore.list_entries 仅取 metadata + filesystem read 拿原 markdown bytes
- ✓ ADR-D12-6 r2 (scheme C dict-form): `_migrate_v1_to_v2_dict_form(data: dict) -> dict` 单参数, 与 `VersionManager.migrate()` (单参数 `migrator(data)`) 兼容; 仅做 schema_version + scope 字段, dst 留给 read_manifest fast-path
- ✓ ADR-D12-7 r2 (commit grouping): T1-T5 5 commits, T2 显式依赖 T1 (helper refactor), T3 独立, T4 独立, T5 docs+sentinel

## 代码质量

- 类型注解: 完整 (新代码 100% type-annotated; 数据类使用 `@dataclass`)
- Docstring: 全部公共函数有 docstring 引 FR + ADR
- 异常处理: `PackInstallError` 一致包装 `git clone` / `subprocess` / `OSError` / `json.JSONDecodeError`
- I/O abstraction: `stderr` / `stdin` / `stdout` 通过依赖注入, 测试可控
- B5 user-pact opt-in: `--yes` (uninstall/update/publish) + `--anonymize` (export) + `--force` (publish bypass sensitive) 全显式
- 无 dead code, 无 commented-out 代码
- ruff baseline diff: **0** (T5 新代码引入 0 新 ruff 错误; pre-existing 478 错误 pattern 完全不变)

## 红线确认

- ✓ CON-1201 (ADR-D12-2 + INV-F12-1): uninstall 触不到 sync-manifest
- ✓ CON-1202 (F009/F010/F011 既有 API 不破): F011 install 测 7 + F012-A install/CLI 测 5 全过
- ✓ CON-1203 (manifest schema 1→2 dict-form 等价): `test_dataclass_and_dict_forms_agree_on_schema_fields`
- ✓ CON-1204 (uninstall 反向 install 字节级反转): atomic + sidecar 反向 + manifest sync
- ✓ CON-1205 (uninstall 不触 .garage 其它组件): 显式注释 + sentinel
- ✓ CON-1206 (零依赖变更): `git diff main..HEAD -- pyproject.toml uv.lock` = 0

## 残余项

- **None blocking**. F011-pattern E402/I001/F841 的 3 个 ruff 错误是 pre-existing 模式 (F011 uninstall handler 同款), F012 引入 0 新错误.

## 通过条件

✅ code review APPROVED, 进入 traceability review.
