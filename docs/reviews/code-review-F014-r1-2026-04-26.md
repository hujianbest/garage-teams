# Code Review — F014 r1 (Workflow Recall)

- **日期**: 2026-04-26
- **审阅人**: Cursor Agent (auto-streamlined per F011/F012/F013-A mode)
- **范围**: T1-T5 src 修改 (4 新模块 + 1 docs/SKILL.md + 4 caller hook + 1 perf script)

## Verdict: APPROVED

## 改动摘要

| 模块 | 增量 LOC | 变更类型 |
|---|---|---|
| `src/garage_os/workflow_recall/__init__.py` | +20 (新) | 顶级包导出 |
| `src/garage_os/workflow_recall/types.py` | +50 (新) | RecallResult + WorkflowAdvisory dataclass |
| `src/garage_os/workflow_recall/cache.py` | +160 (新) | cache.json + last-indexed.json CRUD + atomic write + invalidate |
| `src/garage_os/workflow_recall/path_recaller.py` | +200 (新) | Counter 算法 + 阈值 + window + Im-4 sub-sequence |
| `src/garage_os/workflow_recall/pipeline.py` | +180 (新) | WorkflowRecallHook.invalidate + compute_status_summary |
| `src/garage_os/runtime/session_manager.py` | +9 | Cr-1 Path 1: archive 末尾 hook |
| `src/garage_os/cli.py` | +160 | recall subparser + handler + status 段集成 |
| `src/garage_os/memory/publisher.py` | +7 | Im-1 r2: if-else 末尾 hook |
| `src/garage_os/knowledge/integration.py` | +7 | Path 4: integration store 后 hook |
| `packs/coding/skills/hf-workflow-router/SKILL.md` | +37 | 新 step 3.5 (additive) |
| `packs/coding/skills/hf-workflow-router/references/recall-integration.md` | +110 (新) | router 集成 reference |
| `tests/adapter/installer/dogfood_baseline/skill_md_sha256.json` | 2 hash 更新 | 反映 router SKILL.md 内容变化 |
| `scripts/workflow_recall_perf_smoke.py` | +75 (新) | CON-1403 manual prof |

**总增量**: ~1015 LOC src + ~200 LOC docs/scripts

## 设计-代码对齐性 (检查 ADR-D14-1..7 r2)

- ✓ ADR-D14-1 (顶级包): `src/garage_os/workflow_recall/` 与 sync/skill_mining/ 平级
- ✓ ADR-D14-2 (cache 单文件 JSON): `cache.py::WorkflowRecallCache` 单 cache.json + last-indexed.json; key 格式 `{tt}|{pd}` 用 NULL_SLOT='*' for None
- ✓ ADR-D14-3 r2 (4 caller hook): 4 处真实接入点 (session_manager / cli.py:1741 / publisher.py:144 / integration.py:222); 全 try/except 包; 显式排除 cli.py:1172 + ingest/pipeline.py
- ✓ ADR-D14-4 (Counter on tuple(skill_ids)): `path_recaller::recall` 用 `Counter({seq: len(recs)})` + `most_common(top_k)`; Im-4 `_take_subsequence` 实现 Z 后子序列, Z=last 返回 None
- ✓ ADR-D14-5 (inline status): `cli.py::_print_workflow_recall_status` 在 `_status` 末尾 `_print_skill_mining_status` 之后调用
- ✓ ADR-D14-6 r2 (additive step 3.5 + 2 处 hash): hf-workflow-router/SKILL.md 在 step 3 与 step 4 之间插 step 3.5; dogfood baseline 更 2 keys (.cursor/ + .claude/)
- ✓ ADR-D14-7 (T1-T5 1:1): T1-T5 commit 与 design § 8 任务表对齐

## 代码质量

- 类型注解: 完整 (新代码 100% type-annotated; 数据类 @dataclass)
- Docstring: 全部公共函数有 docstring 引 FR + ADR + spec/reviewer Cr-N r2 修订记录
- 异常处理: try/except 一致包装 4 caller hook; cache load 包 OSError + JSONDecodeError fallback (corrupted → None, 强制 rebuild)
- I/O abstraction: `garage_dir` 通过参数注入, 测试用 tmp_path
- B5 user-pact: `--rebuild-cache` opt-in (用户兜底); platform.json `workflow_recall.enabled` config gate; INV-F14-3 advisory only 文档化
- 无 dead code, 无 commented-out 代码
- ruff baseline diff: **0** (T1-T5 引入 0 新 ruff 错误)

## 红线确认

- ✓ CON-1401 (F003-F013 既有 API 字节级不变): 4 caller 改动全 try/except 包不改 method 签名; F004 ExperienceIndex 不修改
- ✓ CON-1402 (零依赖): `git diff main..HEAD -- pyproject.toml uv.lock` = 0
- ✓ CON-1403 (perf < 2s): manual smoke 1000 records = 0.064s (33x 余量)
- ✓ CON-1404 (router 改不破 3 项守门): dogfood SHA baseline 更; neutrality test PASS; 全套 1043 passed 0 regression
- ✓ CON-1405 (recall 与 recommend 独立): `test_recall_no_recommend_import.py` AST sentinel + code review (`_recall_workflow` 不 import recommend 模块)

## 残余项

- **None blocking**. F011-pattern E402/I001/F841 ruff 错误是 pre-existing 全局模式, F014 引入 0 新错误.

## 通过条件

✅ code review APPROVED, 进入 traceability review.
