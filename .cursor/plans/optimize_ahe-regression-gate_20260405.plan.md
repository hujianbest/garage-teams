---
name: optimize ahe-regression-gate
overview: 提升 `ahe-regression-gate` 的回归范围表达和证据新鲜度约束，重点补强回归面说明、结果记录粒度和旧结果禁用提示，并把本地下一步表达对齐到当前 workflow starter 的 canonical handoff。
---

# 优化 `ahe-regression-gate` 方案

## 目标

让 `ahe-regression-gate` 更稳定地证明“周边没有被这次改动悄悄破坏”。
