---
name: optimize ahe-completion-gate
overview: 提升 `ahe-completion-gate` 的证据类型说明和门禁边界清晰度，重点补强可接受证据、非目标和完成宣告前约束，并把本地下一步表达对齐到当前 workflow starter 的 canonical handoff。
---

# 优化 `ahe-completion-gate` 方案

## 目标

让 `ahe-completion-gate` 更稳定地判断“当前任务是否真的可以宣告完成”。
