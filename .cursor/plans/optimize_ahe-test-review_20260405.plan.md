---
name: optimize ahe-test-review
overview: 提升 `ahe-test-review` 的 fail-first 证据判断和测试质量边界，重点补强证据不足时的处理、与回归门禁的边界，以及测试质量缺口表达。
---

# 优化 `ahe-test-review` 方案

## 目标

让 `ahe-test-review` 更稳定地判断测试是否真的支撑当前实现结论。
